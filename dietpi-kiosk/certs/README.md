# TLS certs for tv-panel (NOT committed)

Place wildcard `*.vls.lan` files here for install.sh:

- `website.crt` → `/etc/tv-panel/cert.pem`
- `website.key` → `/etc/tv-panel/key.pem`

Host: `https://ozii-dash.vls.lan:8787/`
