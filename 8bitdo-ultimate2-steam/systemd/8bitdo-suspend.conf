[Service]
# Задержать freeze, пока геймпад не станет idle (или не истечёт TIMEOUT).
ExecStartPre=/usr/local/bin/8bitdo-pre-suspend.sh
# После resume: снова sleep, если проснулись из-за дока/Home-off.
ExecStopPost=/usr/local/bin/8bitdo-post-resume.sh
